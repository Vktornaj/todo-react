export type User = {
    username: string;
    firstName: string;
    lastName: string;
};

export type UserRegister = {
    username: string;
    firstName: string;
    lastName: string;
    password: string;
};

export type UserLogin = {
    username: string;
    password: string;
};
